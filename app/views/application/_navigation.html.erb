<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">RecruitTracker</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
        <% if current_user %>
        	<li>
            <a class="nav-link" <%= link_to "Home", welcome_path %></a>
	        </li>
					<% if current_user.is_admin? %>
						<li>
	          	<a class="nav-link active" aria-current="page" <%= link_to "Current Recruits", users_path %></a>
		        </li>
					<% end %>
	        <li class="nav-item">
            <a class="nav-link" <%= link_to "Profile", profile_path(id: current_user.id) %></a>
	        </li>
	        <li class="nav-item">
			    	<a class="nav-link" <%= link_to "Logout", logout_path %></a>
	        </li>
        <% else %>
          <li class="nav-item">
            <a class="nav-link" <%= link_to "Sign Up", signup_path %></a>
          </li>
          <li class="nav-item">
            <a class="nav-link" <%= link_to "Log In", login_path %></a>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>